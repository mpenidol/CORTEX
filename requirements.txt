# CORTEX — Python dependencies
# Install: pip install -r requirements.txt

# API / server
fastapi
uvicorn

# LangChain
langchain-core
langchain-ollama
langchain-chroma
langchain-openai          # needed for --provider vllm
langchain-huggingface     # needed for --provider vllm (local embeddings)
einops                    # required by nomic-embed-text-v1.5

# Vector store
chromadb

# Embeddings & reranker
sentence-transformers

# Schemas
pydantic

# Data / plots (only needed for batch + plot_results)
pandas
numpy
matplotlib

# BM25 (needed for --arch bm25 and --arch bm25_rag)
rank-bm25
